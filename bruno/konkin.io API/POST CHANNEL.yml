info:
  name: POST CHANNEL
  type: http
  seq: 21

http:
  method: POST
  url: "{{host}}/api/v1/channels"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
