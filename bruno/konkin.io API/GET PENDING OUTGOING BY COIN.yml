info:
  name: GET PENDING OUTGOING BY COIN
  type: http
  seq: 55

http:
  method: GET
  url: "{{host}}/api/v1/wallets/bitcoin/pending-outgoing"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
