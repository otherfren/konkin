info:
  name: POST WALLET SWEEP
  type: http
  seq: 57

http:
  method: POST
  url: "{{host}}/api/v1/wallets/bitcoin/sweep"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  body:
    type: json
    data: |
      {
        "toAddress": "bc1qexampleaddress0123456789",
        "reason": "Test sweep via REST API"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
