info:
  name: GET WALLET STATUS
  type: http
  seq: 51

http:
  method: GET
  url: "{{host}}/api/v1/wallets/bitcoin/status"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
