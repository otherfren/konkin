info:
  name: GET VOTES FOR REQUEST
  type: http
  seq: 13
http:
  method: GET
  url: '{{host}}/api/v1/requests/req-123/votes'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests:
  - name: Status code is 200
    test: |
      expect(res.getStatus()).to.equal(200);
