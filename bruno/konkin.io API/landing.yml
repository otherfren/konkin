info:
  name: landing
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
