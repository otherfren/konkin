info:
  name: GET WALLET BALANCE
  type: http
  seq: 50

http:
  method: GET
  url: "{{host}}/api/v1/wallets/bitcoin/balance"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
