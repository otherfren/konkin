info:
  name: DELETE VOTE
  type: http
  seq: 27

http:
  method: DELETE
  url: "{{host}}/api/v1/votes/{{vote_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
