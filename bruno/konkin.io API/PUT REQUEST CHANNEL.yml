info:
  name: PUT REQUEST CHANNEL
  type: http
  seq: 33

http:
  method: PUT
  url: "{{host}}/api/v1/request-channels/{{request_channel_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
