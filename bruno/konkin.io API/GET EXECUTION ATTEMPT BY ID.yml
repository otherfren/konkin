info:
  name: GET EXECUTION ATTEMPT BY ID
  type: http
  seq: 36

http:
  method: GET
  url: "{{host}}/api/v1/execution-attempts/{{attempt_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
