info:
  name: GET ALL CHANNELS
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/api/v1/channels"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
