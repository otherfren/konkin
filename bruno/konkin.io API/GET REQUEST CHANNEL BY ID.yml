info:
  name: GET REQUEST CHANNEL BY ID
  type: http
  seq: 32

http:
  method: GET
  url: "{{host}}/api/v1/request-channels/{{request_channel_id}}?X-Api-Key={{API_KEY}}"
  params:
    - name: X-Api-Key
      value: "{{API_KEY}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
