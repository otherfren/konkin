info:
  name: GET ALL REQUESTS
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/api/v1/requests"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
