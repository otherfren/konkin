info:
  name: GET STATE TRANSITION BY ID
  type: http
  seq: 29

http:
  method: GET
  url: "{{host}}/api/v1/state-transitions/{{transition_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
