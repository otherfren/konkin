info:
  name: PUT CHANNEL
  type: http
  seq: 22

http:
  method: PUT
  url: "{{host}}/api/v1/channels/{{channel_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
