info:
  name: GET REQUEST BY ID
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/api/v1/requests/req-123"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
