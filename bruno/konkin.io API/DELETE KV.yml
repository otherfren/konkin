info:
  name: DELETE KV
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/v1/kv/test-key"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
