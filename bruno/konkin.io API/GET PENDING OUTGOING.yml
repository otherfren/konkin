info:
  name: GET PENDING OUTGOING
  type: http
  seq: 54

http:
  method: GET
  url: "{{host}}/api/v1/wallets/pending-outgoing"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
