info:
  name: DELETE STATE TRANSITION
  type: http
  seq: 30

http:
  method: DELETE
  url: "{{host}}/api/v1/state-transitions/{{transition_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
