info:
  name: auth queue
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/auth_queue"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
