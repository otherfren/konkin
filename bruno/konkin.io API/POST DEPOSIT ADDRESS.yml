info:
  name: POST DEPOSIT ADDRESS
  type: http
  seq: 52

http:
  method: POST
  url: "{{host}}/api/v1/wallets/bitcoin/deposit-address"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
