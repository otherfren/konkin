info:
  name: DELETE COIN RUNTIME
  type: http
  seq: 42

http:
  method: DELETE
  url: "{{host}}/api/v1/coin-runtimes/{{coin}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
