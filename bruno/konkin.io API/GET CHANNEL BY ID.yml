info:
  name: GET CHANNEL BY ID
  type: http
  seq: 12

http:
  method: GET
  url: "{{host}}/api/v1/channels/telegram.main"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
