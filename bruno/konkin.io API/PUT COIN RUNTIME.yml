info:
  name: PUT COIN RUNTIME
  type: http
  seq: 41

http:
  method: PUT
  url: "{{host}}/api/v1/coin-runtimes/{{coin}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
