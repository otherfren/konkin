info:
  name: GET KV BY KEY
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v1/kv/test-key"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
