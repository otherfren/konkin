info:
  name: GET COIN RUNTIME BY COIN
  type: http
  seq: 39

http:
  method: GET
  url: "{{host}}/api/v1/coin-runtimes/{{coin}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
