info:
  name: GET ALL REQUEST CHANNELS
  type: http
  seq: 15

http:
  method: GET
  url: "{{host}}/api/v1/request-channels"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
