info:
  name: DELETE CHANNEL
  type: http
  seq: 23

http:
  method: DELETE
  url: "{{host}}/api/v1/channels/{{channel_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
