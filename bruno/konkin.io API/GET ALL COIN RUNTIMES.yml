info:
  name: GET ALL COIN RUNTIMES
  type: http
  seq: 38

http:
  method: GET
  url: "{{host}}/api/v1/coin-runtimes"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
