info:
  name: GET VOTE BY ID
  type: http
  seq: 25

http:
  method: GET
  url: "{{host}}/api/v1/votes/{{vote_id}}"
  headers:
    - name: X-Api-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
